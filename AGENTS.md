# Developer Playbook 작업 지침

## 새 세션 시작

- 이 저장소 작업은 먼저 `README.md`와 `mindset/question-first.md`를 읽는다. 그 뒤 현재 목적에 맞는 문서 하나만 추가로 읽는다.
- 문서 역할의 원본은 다음과 같다: 작업 선택은 `mindset/question-first.md`, 원칙은 `mindset/engineering-philosophy.md`, AI 흐름은 `mindset/ai-workflow.md`, 실행 지시문은 `prompts/`, 반복 질문은 `checklists/`, 실패의 증거·재현 관점은 `failure-patterns/`, 검증된 근거는 `cases/`, 입력 양식은 `templates/`.
- 새 문서를 추가하거나 기존 문서의 역할을 바꾸면 README의 관련 색인도 같은 변경에서 갱신한다. 역할이 겹치면 새 문서를 만들지 말고 위 원본 문서를 보완한다.

## 목적과 경계

- 이 저장소는 프로젝트 원본 기록을 복사하는 곳이 아니라, 검증된 경험에서 일반화한 설계 판단·실패 패턴·체크리스트·AI 활용법을 축적하는 개인 플레이북이다.
- 기술 정의, 사용해 보지 않은 패턴 요약, 프로젝트 고유 정보, 원문 로그는 기록하지 않는다.
- 실제 프로젝트의 상세 사실은 원래 프로젝트의 `context/`에만 남긴다. 이 저장소에서 원본을 고치거나 동기화하지 않는다.

## 반영 요청 처리

사용자가 `플레이북 반영`, `승격`, `export 반영`처럼 명시적으로 요청한 경우에만 아래 흐름으로 작업한다.

1. 사용자가 지정한 원본 경로와 식별 가능한 후보 하나를 입력 계약으로 받는다. 원본 프로젝트의 위치·후보를 추측하거나 전체 프로젝트를 기본 탐색하지 않는다.
2. 먼저 이 저장소의 Git 상태를 확인하고 관련 없는 변경은 보존한다. `README.md`, `mindset/question-first.md`, 대상 주제의 기존 문서만 읽어 중복을 검사한다.
3. 후보의 근거, 일반화 가능성, 비밀·개인·프로젝트 식별 정보, 제품 고유 구현, 미해결 취약점을 점검한다. 근거가 부족하거나 민감 정보가 남으면 반영하지 않고 이유를 보고한다.
4. 아래 목적지 규칙에 따라 기존 문서를 수정하거나 새 문서를 하나만 만든다.
5. 변경 내용과 검증 근거를 간결히 보고한다. 사용자가 요청하지 않은 커밋·푸시·공개 저장소 변경은 하지 않는다.
6. 실제 반영이 끝난 경우에만 원본 `playbook-export.md`의 정확한 후보 상태를 `로컬 반영`으로 바꾸고 대상 문서·날짜를 남긴다. 커밋·푸시는 별도 요청이며, 완료된 경우에만 `게시 완료`로 갱신한다.

사용자가 `초안만`, `검토만`이라고 요청하면 1~3까지만 수행하고 파일을 수정하지 않는다.

## 목적지 선택 규칙

- `prompts/`: 여러 프로젝트에서 재사용할 AI 요청문과 사용 시점.
- `cases/`: 검증된 하나의 경험에서 맥락·불변조건·대안·검증까지 남길 가치가 있을 때.
- `checklists/`: 독립된 경험 세 번 이상에서 반복되었거나, 한 번이라도 높은 위험의 재발 방지 규칙일 때.
- `references/`: 직접 확인한 공식 문서·이슈·회고와 적용 시 주의점을 함께 남길 때.

처음에는 빈 폴더나 문서를 만들지 않는다. 기존 문서에 자연스럽게 추가할 수 있으면 새 파일을 만들지 않는다.

## 문서 품질

- 사건의 설명보다 재사용 가능한 판단을 우선한다. 사실, 해석, 미확정 가설을 구분한다.
- “항상”, “무조건” 같은 규칙은 근거와 적용 조건이 있을 때만 사용한다.
- 사례 문서에는 최소한 맥락, 불변조건, 대안·결정, 검증, 다음 질문을 남긴다.
- 같은 판단을 `cases/`, `checklists/`, `prompts/`에 전문으로 중복하지 않는다. 한 문서를 원본으로 두고 다른 문서에는 짧은 링크나 요약만 남긴다.
- 실제 경로, 도메인, 계정·팀원·티켓 식별자, 비밀값, 내부 로그는 반영하지 않는다.
- 수정되지 않은 보안 취약점, 고객·운영 데이터, 제품 고유 알고리즘·정책은 일반화해도 공개에 부적절할 수 있으므로 반영하지 않는다.
- 사례는 적용 범위·비적용 조건·검증 근거를 함께 남긴다. 실패 패턴은 증상·유발 조건·최소 재현·최초 근거를, 체크리스트는 구현 전후의 짧은 질문만 둔다.

## 자동화의 의미

자동화는 스크립트가 두 저장소를 복사하는 방식이 아니라, 명시적 승격 요청 때 에이전트가 후보 선별·민감정보 제거·중복 검사·문서 초안·로컬 반영까지 수행하는 방식이다. 내용의 사실성·일반화 판단은 사람이 최종 검토한다.

# Concurrency

> 상태: 초기 가설. 실제 사례와 연결되면 갱신한다.

## 증상

수량·상태가 기대와 다르거나 같은 자원이 중복 생성된다.

## 유발 조건

둘 이상의 요청이 같은 자원을 읽은 뒤, 서로의 변경을 모른 채 수정한다.

## 최소 재현

핵심 불변조건 하나를 정하고, 두 요청이 읽기와 쓰기 사이에서 교차하도록 동시 실행한다.

## 최초 근거

요청별 식별자, 변경 전후 값, DB 제약·락 대기, 동시 테스트 결과를 확인한다.

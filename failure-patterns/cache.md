# Cache

> 상태: 초기 가설. 실제 사례와 연결되면 갱신한다.

## 증상

원본과 다른 값이 보이거나, 캐시 만료·장애 순간에 원본 시스템 부하가 급증한다.

## 유발 조건

원본 변경과 캐시 무효화 순서가 불명확하거나, 많은 키가 동시에 만료된다.

## 최소 재현

원본 변경 직후 조회, 캐시 장애, 동시 만료 상황을 각각 만들고 값·원본 호출 수를 비교한다.

## 최초 근거

캐시 적중률, 원본 조회량, TTL, 무효화 이벤트와 사용자 응답 값을 함께 확인한다.
